vue init templateName projectName
vue init vueschool/webpack-template vueschool-forum
route ta mode: "history" yaooinca direk hashing kalkiyor
child parent a bilgi gonderirken custom events kullaniliyor 
    $emit function to specify custome events 
    this.$emit("NAMEOFEVENT", list,of,arguments,a,b,c,) arkkasindan @NAMEOFEVENT="NAMEOFMETHOD" diyerek parent component can listen custome events 
/* @import "~bootstrap/css/bootstrap.css" gibi, index html de yazmamiz burda yazmamizin nedeni webpack sikistirsin css file ni diye*/  /* if we want to import from node_module we we can use ~ (shift+`) symbol */ 
